<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- ===== Console Appender ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <target>System.out</target>
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %highlight([%level]) [%thread] %logger{5} %M - %msg%n%ex
            </pattern>
        </encoder>
    </appender>

    <!-- ===== Rolling File Appender ===== -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/websites-watcher.log</file>
        <append>true</append>

        <!-- JSON layout using logstash-logback-encoder -->
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>time</fieldName>
                    <pattern>yyyy-MM-dd'T'HH:mm:ss.SSSXXX</pattern>
                </timestamp>
                <pattern>
                    <pattern>
                        {
                            "level": "%level",
                            "thread": "%thread",
                            "logger:": "%logger",
                            "message": "%message",
                            "exception": "%exception"
                        }
                    </pattern>
                </pattern>
            </providers>
        </encoder>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>
                logs/websites-watcher-%d{yyyy-MM-dd}.%i.log.zip
            </fileNamePattern>

            <!-- SizeBasedTriggeringPolicy -->
            <maxFileSize>10MB</maxFileSize>

            <!-- DefaultRolloverStrategy max=30 -->
            <maxHistory>30</maxHistory>

            <!-- Optional: cap total size -->
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- ===== Async Appender ===== -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>8192</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
        <appender-ref ref="FILE"/>
    </appender>

    <!-- ===== Root Logger ===== -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>

</configuration>